{
  "name": "highwayman",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "server",
    "client",
    "runner",
    "docs"
  ],
  "scripts": {
    "dev": "node scripts/dev.js",
    "build": "npm run build -w client",
    "start": "npm run start -w server",
    "serve:public": "node scripts/serve-public.js",
    "cli": "npm run start -w runner",
    "runner": "npm run start -w runner",
    "docs:dev": "npm run dev -w docs",
    "docs:build": "npm run build -w docs"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
